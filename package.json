{
  "name": "travel",
  "version": "1.0.0",
  "description": "- `/frontend` — React app\r - `/backend` — Node/Express API",
  "scripts": {
    "dev": "concurrently \"npm run server --prefix backend\" \"npm run start --prefix frontend\"",
    "start": "concurrently \"npm start --prefix backend\" \"npm start --prefix frontend\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
